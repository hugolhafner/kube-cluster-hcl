replicaCount: 3

extraArgs:
  - --enable-insecure-login

protocolHttp: true

service:
  externalPort: 80

resources:
  requests:
    cpu: 300m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 128Mi